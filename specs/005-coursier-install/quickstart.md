# Quickstart

## Installation

To install `first` via Coursier:

```bash
cs install --contrib https://raw.githubusercontent.com/oswaldo/first/main/channel first
```

## Verification

Check the installed version:

```bash
first --version
```
