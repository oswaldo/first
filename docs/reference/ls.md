# List Command (`ls`)

List all available contexts known to `first`.

## Usage

```bash
first ls [options]
```

## Description

Scans the local storage (usually `~/.first/`) and lists all valid `.fctx` files found.
It displays the name of the context (filename without extension) and potentially metadata in future versions.
